# Server Configuration
NODE_ENV=development
DOMAIN="http://localhost:8080"
API_PORT=3000
VUE_PORT=8080

# Database (Local Development)
DB_USER=arithmetix_admin
DB_HOST=localhost
DB_NAME=arithmetix_db
DB_PASSWORD=admin
DB_PORT=5432

# Database (Production - Cloud Platforms)
# Use DATABASE_URL instead of individual DB variables
# DATABASE_URL=postgresql://user:password@host:port/database

# JWT Secret (Change in production!)
SECRET=supersecret

# Email Configuration (Office 365 / Outlook)
EMAIL_SERVER=smtp.office365.com
EMAIL_PORT=587
EMAIL_ADDRESS=
EMAIL_USERNAME=
EMAIL_PASSWORD=
EMAIL_OFFICE_RECIPIENTS=